/*
  Constants used exclusively by
  the files in this directory.
*/

const BASE_URL = 'realdevsquad.com';

export const links = [
	{ href: `welcome.${BASE_URL}`, name: 'welcome' },
	{ href: `${BASE_URL}/events`, name: 'events' },
	{ href: `members.${BASE_URL}`, name: 'members' },
	{ href: `crypto.${BASE_URL}`, name: 'crypto' },
	{ href: `status.${BASE_URL}`, name: 'status' },
];
